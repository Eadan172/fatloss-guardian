services:
  fatloss-guardian:
    build: .
    container_name: fatloss-guardian
    ports:
      - "8080:80"
    volumes:
      # 本机备份文件挂载（可读写）：任何模式下数据变化都会写入 data/backup.json，启动自动恢复
      - ./data:/usr/share/nginx/html/data
    restart: unless-stopped
